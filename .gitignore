# IDEA
.idea/
*.iml

# AI / Agent tooling
.opencode/
.lean-ctx/
.claude/
.mcp/
.agent/
.ai/
graphify-out/
# Session archive (per-branch contract snapshots — regenerated by scripts/snapshot-contract.sh)
session/

# OS
.DS_Store
Thumbs.db

# Environment (secrets — never commit)
.env

# General
*.log
*.bak
*.swp
*.swo
*~

# Node
node_modules/

# Plugins (installed by skill system)
plugins/

# Task board (sprint tracking, not version-controlled)
tasks/

# Contract runtime state (mutates on every orchestration session — don't commit)
contract/contract.json
contract/state.md
# Template artifacts (goods-price-comparison ported files)
AGENTS.md
CLAUDE.md
