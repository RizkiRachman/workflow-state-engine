# IDEA
.idea/
*.iml

# AI / Agent tooling
.opencode/
.lean-ctx/
.claude/
.mcp/
.agent/
.ai/
graphify-out/
# Session archive (per-branch contract snapshots — regenerated by scripts/snapshot-contract.sh)
session/

# Progress analysis artifacts (session runtime — don't commit)
backlog/

# OS
.DS_Store
Thumbs.db

# Environment (secrets — never commit)
.env

# General
*.log
*.bak
*.swp
*.swo
*~

# Node
node_modules/

# Plugins (installed by skill system)
plugins/

# Task board (sprint tracking, not version-controlled)

session/feature/
session/main/
session/index.md
session/state.md


# Contract runtime state — session/<branch>/contract.json for per-session envelopes
# contract/ itself contains only committed definitions
# Template artifacts (goods-price-comparison ported files)
AGENTS.md
CLAUDE.md
